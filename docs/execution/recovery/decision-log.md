# Decision log

| ID | Decision | Rationale | Date |
|---|---|---|---|
| D-001 | Execute M01 only in this pass; leave M02–M14 `not_started`/`blocked` | Faithful execution discipline; do not overstate completion | 2026-08-03 |
| D-002 | Create recovery branch locally, do not push | Prompt forbids push/merge/default-branch change without explicit authorisation | 2026-08-03 |
| D-003 | Treat missing AI-Native Hiring Blueprint as hard blocker B-001 for M06/M07/M10 content | Prompt: do not invent assessment content when a requirement is authoritative | 2026-08-03 |
| D-004 | In M02, defer two breaking steps to the M14 cutover: (a) rename `@cpf/web`→`@cpf/legacy-web`; (b) remove Candidate V2 / Reviewer V2 route registrations from `apps/web/src/main.tsx` | Their replacements (`candidate-web` M07, `reviewer-web` M09) do not exist yet, and a live demo depends on the current `@cpf/web` routes. Removing now breaks the candidate/reviewer experience with no replacement, violating the "legacy V1 remains runnable" acceptance criterion. Ownership rules (CODEOWNERS + boundaries doc + legacy README) already prevent NEW role code entering the legacy app — the substantive M02 guarantee. | 2026-08-03 |
| D-005 | Align `@cpf/admin-web` (project/) fully to React 19 rather than keeping React 18 | Hoisting into the single root install created a dual-`@types/react` (18 vs 19) conflict (bigint-in-ReactNode). Unifying on React 19 (react-router-dom 6.30 supports it; lucide-react bumped 0.344→0.469 for the React 19 peer) resolves the type skew and satisfies M02's "align React/TS/Vite versions" requirement. react-router-dom kept at v6 (v6→v8 is an API rewrite) — router major-version alignment deferred as a separate tested migration. | 2026-08-03 |
| D-006 | Move `project/` → `apps/admin-web/` (supersedes the M02 note to keep the physical folder as `project/`) and clean the repo | Direct user request for a consistent, well-structured repo: all four web apps now live under `apps/*` (`admin-web`, `candidate-web`, `reviewer-web`, `web`). Also removed trash: two 0-byte root seed files (`seed-local.mjs`, `seed-platform-demo.mjs`), 11 unreferenced 0-byte `src` stubs in admin-web, the redundant nested Vite `.gitignore`, and gitignored `dist/` build outputs; foldered loose scripts into `scripts/pilot` and `scripts/uat`. Package name stays `@cpf/admin-web`, so root `-w @cpf/admin-web` scripts are unaffected. Empty migration `0022_platform_directory_read_all.sql` left untouched (immutable applied history). Note: deleting packages' `dist/` transiently crashed the running API/web (they consume built `dist/`); resolved by rebuild + restart. | 2026-08-03 |
