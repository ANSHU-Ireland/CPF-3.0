{
  "name": "@cpf/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CPF Enterprise Ecosystem API — modular monolith entry point. Currently serves the versioned assessment framework catalogue and stateless scoring evaluation. Tenant, identity, and personal-data endpoints are intentionally not implemented yet (see docs/status/completion-report.md).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "retention:dry-run": "node dist/jobs/retention.js",
    "retention:execute": "node dist/jobs/retention.js --execute",
    "notify:retry": "node dist/jobs/notify.js",
    "openapi:generate": "node scripts/generate-openapi.mjs"
  },
  "dependencies": {
    "@cpf/ai-gateway": "*",
    "@cpf/assessment-framework": "*",
    "@cpf/assessment-packs": "*",
    "@cpf/identity": "*",
    "@cpf/v2-contracts": "*",
    "fastify": "^5.2.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.13.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@types/node": "^22.10.2",
    "@types/nodemailer": "^8.0.1",
    "@types/pg": "^8.11.10",
    "pino": "^10.3.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
